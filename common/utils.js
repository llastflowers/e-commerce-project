export const makePrettyCurrency = (number) => number.toLocaleString('en-US', {
    style: 'currency', 
    currency: 'JPY', 
});

const totalItemPrice (item, quantity) => fruit.price * quantity;

export const totalCartPrice = (items, cart) => {

}